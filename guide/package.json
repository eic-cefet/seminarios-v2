{
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "pnpm exec playwright install chromium && pnpm exec tsx generate.ts",
    "generate:quick": "pnpm exec tsx generate.ts",
    "serve": "pnpm exec tsx serve.ts"
  },
  "devDependencies": {
    "playwright": "^1.50.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
